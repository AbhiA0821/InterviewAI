"""
firebase_auth.py
-----------------
Handles verification of Firebase Google Authentication tokens.

Responsibilities (future implementation):
    - Initialize the Firebase Admin SDK
    - Verify incoming ID tokens from the frontend
    - Extract user identity (uid, email, name, picture) from the token

Used as a FastAPI dependency to protect authenticated routes.
"""

# TODO: implement Firebase Admin SDK initialization and token verification
