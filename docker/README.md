# docker/

Holds shared/auxiliary Docker assets that aren't specific to just the
backend or frontend (e.g. an nginx reverse-proxy config for local
multi-service setups, or a docker-compose override file for
production-like local testing).

The primary Dockerfiles live alongside their respective services:
    - backend/Dockerfile
    - frontend/Dockerfile

Nothing here yet — placeholder for future shared Docker configuration.
