/**
 * InterviewContext.tsx
 * -----------------------
 * React context providing shared state for the active interview
 * flow (selected resume, target role, session id, transcript).
 */

// TODO: implement InterviewProvider and InterviewContext
