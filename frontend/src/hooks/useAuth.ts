/**
 * useAuth.ts
 * -----------
 * Hook exposing the current Firebase-authenticated user and
 * auth actions (signIn, signOut). Implementation deferred to the
 * Authentication phase.
 */

// TODO: implement useAuth hook, likely reading from AuthContext
