/**
 * MainLayout.tsx
 * -----------------
 * Layout wrapper for authenticated app pages (navbar, content area,
 * footer).
 */

export default function MainLayout() {
  return <div>Main Layout (placeholder)</div>;
}
