/**
 * RoleSelectionPage.tsx
 * ------------------------
 * Lets the user choose a target job role for the interview.
 */

export default function RoleSelectionPage() {
  return <div>Role Selection Page (placeholder)</div>;
}
